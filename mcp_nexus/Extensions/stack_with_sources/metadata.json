{
  "name": "stack_with_sources",
  "description": "Downloads stack trace with source code for all frames. Gets the stack trace and runs lsa on each return address to download source files.",
  "version": "1.0.0",
  "author": "MCP Nexus Team",
  "scriptType": "powershell",
  "scriptFile": "stack_with_sources.ps1",
  "timeout": 1800000,
  "requires": ["McpNexusExtensions"],
  "parameters": [
    {
      "name": "threadId",
      "type": "string",
      "description": "Thread ID to analyze (e.g., '8' or '.' for current thread)",
      "required": false,
      "default": "."
    }
  ]
}

